package ${context.basePackage}.mapper;

import ${context.className};

/**
 * ${context.simpleClassName} Mapper
 *
 * @author ${context.author}
 * @date ${context.dateTime}
 */
@Mapper
public interface ${context.simpleClassName}Mapper extends BaseMapper<${context.simpleClassName}> {

}
